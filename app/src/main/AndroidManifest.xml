<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.askhmer.chat">

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="22" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyTabTheme">
        <activity android:name=".activity.PhoneLogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivityTab"></activity>
        <activity android:name=".activity.VerifyCode" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.GroupChat" />
        <activity android:name=".activity.SecretChat" />
        <activity android:name=".activity.Chat" />
        <activity
            android:name=".activity.UserProfile"
            android:label="@string/title_activity_user_profile"
            android:parentActivityName=".activity.MainActivityTab"
            android:theme="@style/MyTabTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.askhmer.chat.activity.MainActivityTab" />
        </activity>
        <activity android:name="com.soundcloud.android.crop.CropImageActivity" />
        <activity android:name=".activity.SingUp"></activity>
    </application>

</manifest>
